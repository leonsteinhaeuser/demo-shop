apiVersion: argoproj.io/v1alpha1
kind: Application
metadata:
  name: cart-presentation
  labels:
    app.kubernetes.io/name: demo-shop-cart-presentation
    app.kubernetes.io/part-of: demo-shop
    app.kubernetes.io/component: backend-service
  finalizers:
    - resources-finalizer.argocd.argoproj.io
spec:
  project: demo
  source:
    repoURL: ghcr.io/leonsteinhaeuser/demo-shop
    chart: cart-presentation
    targetRevision: "v0.4.3"
    helm:
      values: |
        replicaCount: 3

        image:
          repository: ghcr.io/leonsteinhaeuser/demo-shop-cartpresentation
          tag: v0.4.3

        resources:
          limits:
            cpu: 100m
            memory: 64Mi
          requests:
            cpu: 100m
            memory: 64Mi

        autoscaling:
          enabled: true
          minReplicas: 1
          maxReplicas: 20
          targetCPUUtilizationPercentage: 80

        upstreamServiceUrls:
          cartService: http://cart:8080
          itemService: http://item:8080

        podMonitor:
          enabled: true
          namespace: ""
          labels: {}
          interval: 30s
          scrapeTimeout: 10s
          path: /metrics
          relabelings: []
          metricRelabelings: []

        # Pod Disruption Budget configuration
        podDisruptionBudget:
          enabled: true
          minAvailable: 1
          labels: {}

        tracing:
          enabled: true
          endpoint: jaeger.jaeger:4317
          insecure: true
          protocol: grpc

  destination:
    server: https://kubernetes.default.svc
    namespace: demo-shop

  syncPolicy:
    automated:
      prune: true
      selfHeal: true
      allowEmpty: false
    syncOptions:
      - CreateNamespace=true
      - PrunePropagationPolicy=foreground
      - PruneLast=true
    retry:
      limit: 5
      backoff:
        duration: 5s
        factor: 2
        maxDuration: 3m

  revisionHistoryLimit: 10

apiVersion: argoproj.io/v1alpha1
kind: Application
metadata:
  name: cart-qa
  labels:
    app.kubernetes.io/name: demo-shop-cart
    app.kubernetes.io/part-of: demo-shop
    app.kubernetes.io/component: backend-service
  finalizers:
    - resources-finalizer.argocd.argoproj.io
spec:
  project: demo-qa
  source:
    repoURL: ghcr.io/leonsteinhaeuser/demo-shop
    chart: cart
    targetRevision: "v0.5.0"
    helm:
      values: |
        replicaCount: 1

        image:
          repository: ghcr.io/leonsteinhaeuser/demo-shop-cart
          tag: v0.5.0

        resources:
          limits:
            cpu: 100m
            memory: 64Mi
          requests:
            cpu: 100m
            memory: 64Mi

        serviceMonitor:
          enabled: true
          namespace: ""
          labels: {}
          interval: 30s
          scrapeTimeout: 10s
          path: /metrics
          relabelings: []
          metricRelabelings: []

        tracing:
          enabled: true
          endpoint: jaeger.jaeger:4317
          insecure: true
          protocol: grpc

  destination:
    server: https://kubernetes.default.svc
    namespace: demo-shop-qa

  syncPolicy:
    automated:
      prune: true
      selfHeal: true
    syncOptions:
      - CreateNamespace=true
      - PrunePropagationPolicy=foreground
      - PruneLast=true
    retry:
      limit: 5
      backoff:
        duration: 5s
        factor: 2
        maxDuration: 3m

  revisionHistoryLimit: 10

# Frontend Containerfile for Demo Shop Dashboard
FROM nginx:alpine

# Install envsubst and curl if not available
RUN apk add --no-cache gettext curl

# Remove default nginx website
RUN rm -rf /usr/share/nginx/html/*

# Copy frontend files
COPY frontend/ /usr/share/nginx/html/

RUN chmod -R g+rwX /usr/share/nginx/html /var/cache/nginx && \
    chown -R 0:0 /usr/share/nginx/html /var/cache/nginx

# Copy custom nginx configuration
COPY frontend/nginx.conf /etc/nginx/nginx.conf

# Copy and make executable the entrypoint script
COPY frontend/docker-entrypoint.sh /docker-entrypoint.sh
RUN chmod +x /docker-entrypoint.sh

# Copy health check script
COPY frontend/health-check.sh /usr/local/bin/health-check.sh
RUN chmod +x /usr/local/bin/health-check.sh

# Expose port 8080
EXPOSE 8080

# Add health check
HEALTHCHECK --interval=30s --timeout=10s --start-period=5s --retries=3 \
    CMD curl -f http://localhost:8080/health/liveness || exit 1

# Start with configuration generation
CMD ["/docker-entrypoint.sh"]

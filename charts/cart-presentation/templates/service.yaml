apiVersion: v1
kind: Service
metadata:
  name: {{ include "cart-presentation.fullname" . }}
  labels:
    {{- include "cart-presentation.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "cart-presentation.selectorLabels" . | nindent 4 }}

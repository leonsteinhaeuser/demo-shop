apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "cart-presentation.fullname" . }}-test-connection"
  labels:
    {{- include "cart-presentation.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "cart-presentation.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
